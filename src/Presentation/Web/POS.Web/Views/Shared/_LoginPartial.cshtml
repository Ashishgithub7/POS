<ul class="nav flex-column mt-auto mb-2">
    <li class="nav-item">
        <a class="nav-link text-white" data-bs-toggle="collapse" href="#accountSubmenu" role="button" aria-expanded="false" aria-controls="accountSubmenu">Account</a>

        <div class="collapse" id="accountSubmenu">
            <ul class="nav flex-column ms-3">
                @if (User.Identity != null && User.Identity.IsAuthenticated)
                {
                    @if ((await _authorizationService.AuthorizeAsync(User,Policy.UserCreate)).Succeeded)
                    {
                        <li class="nav-item">
                            <a class="nav-link text-white" asp-area="" asp-controller="Account" asp-action="Register">Register</a>
                        </li>
                        
                    }

                    <li class="nav-item">
                        <form asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                            <button type="submit" class="btn btn-link text-start nav-link text-white" style="text-decoration:none">Logout</button>
                        </form>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        @* <a class="nav-link text-white" asp-area="" asp-controller="Account" asp-action="Login" asp-route-returnUrl="@Context.Request.Path">Login</a> *@
                        <a class="nav-link text-white" asp-area="" asp-controller="Account" asp-action="Login">Login</a>
                    </li>
                }
                
            </ul>

        </div>
    </li>
</ul>